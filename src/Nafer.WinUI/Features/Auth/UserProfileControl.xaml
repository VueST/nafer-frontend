<UserControl
    x:Class="Nafer.WinUI.Features.Auth.UserProfileControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:auth="using:Nafer.WinUI.Features.Auth"
    x:DataType="auth:UserProfileControl">

    <UserControl.Resources>
        <Style x:Key="AccountMenuItemStyle" TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Height" Value="36" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="UseSystemFocusVisuals" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter x:Name="ContentPresenter"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Content="{TemplateBinding Content}"
                            ContentTransitions="{TemplateBinding ContentTransitions}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                            AutomationProperties.Name="{TemplateBinding AutomationProperties.Name}">
                            
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </ContentPresenter>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel Padding="12" Spacing="4">

        <!-- Header: Profile Info -->
        <Grid Margin="4,4,4,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <PersonPicture
                Width="40" Height="40"
                Margin="0,0,12,0"
                Initials="{x:Bind ViewModel.UserInitials, Mode=OneWay}" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center" UseLayoutRounding="True">
                <!-- Line 1: Name and Premium Badge -->
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock
                        Text="{x:Bind ViewModel.UserName, Mode=OneWay}"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        FontSize="14"
                        VerticalAlignment="Center"
                        TextTrimming="CharacterEllipsis" />
                    
                    <Border
                        VerticalAlignment="Center"
                        CornerRadius="4"
                        Padding="6,2,6,3"
                        Background="{x:Bind GetBadgeBackgroundColor(ViewModel.UserRole), Mode=OneWay}"
                        BorderThickness="0"
                        Visibility="{x:Bind ViewModel.IsRoleBadgeVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock
                            Text="{x:Bind ViewModel.RoleBadgeLabel, Mode=OneWay}"
                            FontSize="10"
                            CharacterSpacing="25"
                            FontWeight="Bold"
                            VerticalAlignment="Center"
                            Foreground="{x:Bind GetBadgeForegroundColor(ViewModel.UserRole), Mode=OneWay}" />
                    </Border>
                </StackPanel>

                <!-- Line 2: Email Address -->
                <TextBlock
                    Margin="0,2,0,0"
                    Text="{x:Bind ViewModel.UserEmail, Mode=OneWay}"
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Grid>

        <MenuFlyoutSeparator Margin="0,0,0,4" />

        <!-- Menu Action: Library -->
        <Button Style="{StaticResource AccountMenuItemStyle}">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xE8F1;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" 
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="My Library" VerticalAlignment="Center" FontSize="13"/>
            </StackPanel>
        </Button>

        <!-- Menu Action: Notifications -->
        <Button Style="{StaticResource AccountMenuItemStyle}">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xEA8F;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" 
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="Notifications" VerticalAlignment="Center" FontSize="13"/>
            </StackPanel>
        </Button>

        <MenuFlyoutSeparator Margin="0,4" />

        <!-- Sign Out Button -->
        <Button Style="{StaticResource AccountMenuItemStyle}"
                Command="{x:Bind ViewModel.LogoutCommand}">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xF3B1;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" 
                          Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                <TextBlock Text="Sign Out" VerticalAlignment="Center" FontSize="13"
                           Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
            </StackPanel>
        </Button>

    </StackPanel>
</UserControl>
